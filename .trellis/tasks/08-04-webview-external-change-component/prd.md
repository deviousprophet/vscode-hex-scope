# Extract external-change banners into self-contained component

## Goal

Child of webview-component-refactor (#151). Conflict/reload/error banners (externalChangeUi.ts): render + dismiss + action buttons. Colocated .ext-*-banner CSS (currently in toolbar.css). Lock-state (disable/enable interactive elements) stays host/util.

## Requirements

- TBD

## Acceptance Criteria

- [ ] TBD

## Notes

- Keep `prd.md` focused on requirements, constraints, and acceptance criteria.
- Lightweight tasks can remain PRD-only.
- For complex tasks, add `design.md` for technical design and `implement.md` for execution planning before `task.py start`.
