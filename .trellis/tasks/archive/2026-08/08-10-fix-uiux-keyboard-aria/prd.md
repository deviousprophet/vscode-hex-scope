# UI/UX: Keyboard + ARIA pass (hex grid nav, context menu, labels list)

## Goal

From ui-ux-review.md (M): hex grid is mouse-only (no keyboard selection); context menu has no role=menu/arrow keys/focus/Menu-key; labels list actions are span-only with hover-reveal (not focusable/touch); icon-only buttons lack aria-labels (profiles CRUD, struct move/delete, script run). Keyboard-operable + focusable + aria-labeled.

## Requirements

- TBD

## Acceptance Criteria

- [ ] TBD

## Notes

- Keep `prd.md` focused on requirements, constraints, and acceptance criteria.
- Lightweight tasks can remain PRD-only.
- For complex tasks, add `design.md` for technical design and `implement.md` for execution planning before `task.py start`.
