# UX a11y: hit targets + toggle strip

## Goal

Fix: disclosure button full-row clickable (sidebar.ts, button width auto, h3 flex:1 dead zone); toggle hit size ~13px < 24px WCAG 2.2 (add vertical padding); role=region body needs aria-labelledby (sidebar.ts). Note: the run-button fake aria-disabled item is DONE — real `disabled` landed in the Scripts rework (c933ea0).

## Requirements

- TBD

## Acceptance Criteria

- [ ] TBD

## Notes

- Keep `prd.md` focused on requirements, constraints, and acceptance criteria.
- Lightweight tasks can remain PRD-only.
- For complex tasks, add `design.md` for technical design and `implement.md` for execution planning before `task.py start`.
