# UI/UX: Record view empty-state copy is misleading

## Goal

From ui-ux-review.md (M): when recordCount===0, hexViewer.ts:248-250 shows RECORD_EMPTY_MESSAGE ('Record details are not loaded...') under 'Record View Unavailable' - reads like a feature limitation, not an empty file. Render a real empty state when recordCount===0.

## Requirements

- TBD

## Acceptance Criteria

- [ ] TBD

## Notes

- Keep `prd.md` focused on requirements, constraints, and acceptance criteria.
- Lightweight tasks can remain PRD-only.
- For complex tasks, add `design.md` for technical design and `implement.md` for execution planning before `task.py start`.
