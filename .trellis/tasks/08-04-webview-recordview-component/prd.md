# Extract RecordView into self-contained component

## Goal

Child of webview-component-refactor (#151). Record table: markup, paging, virtual scroll via shared render/virtualScroll, colocated record-view.css.

## Requirements

- TBD

## Acceptance Criteria

- [ ] TBD

## Notes

- Keep `prd.md` focused on requirements, constraints, and acceptance criteria.
- Lightweight tasks can remain PRD-only.
- For complex tasks, add `design.md` for technical design and `implement.md` for execution planning before `task.py start`.
